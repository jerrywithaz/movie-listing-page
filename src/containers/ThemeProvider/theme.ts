const theme = {
    palette: {
        common: {
            black: "#000000",
            white: "#ffffff"
        },
        gray: {
            primary: "#E5E5E5"
        }
    }
};

export type Theme = typeof theme;

export default theme;